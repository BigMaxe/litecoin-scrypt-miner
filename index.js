import React from 'react';
import { AppRegistry } from 'react-native';
import App from './app/ui/app';

const Main = () => {
  return <App />;
};

AppRegistry.registerComponent('LitecoinScryptMiner', () => Main);
